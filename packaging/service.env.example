ENVIRONMENT=production

# aops-cli is installed by the operator. AOPS_API_KEY is never configured here;
# Studio receives it per TEST request and only passes it to the child process.
AOPS_BASE_URL=https://aops.internal/aops/api
AOPS_CA_BUNDLE=/etc/pki/tls/certs/ca-bundle.crt
AOPS_CLI_PATH=/usr/local/bin/aops-cli
AOPS_CLI_VERSION_REQUIREMENT=

# Used only by workflow draft compilation.
LLM_BASE_URL=http://llm.internal/v1
LLM_API_KEY=
LLM_MODEL=
LLM_TIMEOUT_SECONDS=30
LLM_PATH=/chat/completions
LLM_RESPONSE_FORMAT=auto

# Optional bearer token for tec01 -> Runtime internal APIs.
RUNTIME_SERVICE_TOKEN=

# Required to enter the standalone Studio and manage Node settings.
STUDIO_ADMIN_TOKEN=replace-with-long-random-admin-token
STUDIO_SESSION_HOURS=8
STUDIO_COOKIE_SECURE=false

# TEST_ONLY data; never a production workflow state store.
STUDIO_DATABASE_PATH=/var/lib/itsm-workflow/studio.db
STUDIO_TTL_HOURS=24
STUDIO_ARTIFACT_MAX_BYTES=10485760
STUDIO_DATABASE_MAX_BYTES=536870912

# Configure when tec01 will submit compiler and execution jobs.
TEC01_BASE_URL=
TEC01_SERVICE_TOKEN=
TEC01_TIMEOUT_SECONDS=20
EXECUTOR_ID=executor-01
EXECUTOR_MAX_ACTIVE_RUNS=32
EXECUTOR_MAX_ACTIVE_CLI=8

NODE_TIMEOUT_SECONDS=600
STATIC_DIR=frontend/dist
WORKFLOW_BIND_HOST=0.0.0.0
WORKFLOW_PORT=8089
WORKFLOW_BASE_PATH=
